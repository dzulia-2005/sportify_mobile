import React from 'react';
import { Text } from 'react-native';
import { styles } from '../styles/TeamsStyles';

const NotFoundComponent = () => {
  return <Text style={styles.notFound}>Not found Teams</Text>;
};

export default NotFoundComponent;
