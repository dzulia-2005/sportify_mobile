export type AddMatchModalProps = {
  visible: boolean;
  onClose: () => void;
};

export type addTournamentType = {
  name: string;
  startDate: string;
  endDate: string;
  mySchoolId: string;
  matchType: number;
};
